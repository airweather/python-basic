# 모듈
# 함수, 상수(변수) 또는 클래스를 주제지향적으로 모아 놓은 집합체

# 모듈 : 클ㅐ스, 함수, 상수의 집합
# 패키지 : 하위 패키지 및 모듈의 집합
# 라이브러리 : 패키지 및  모듈의 집합

# 모듈 등록 1
# import 모듈이름 [as 별칭]
# 파이썬 모듈을 프로그램 내부에서 사용할 네임스페이스에 추가하는 명령어
# 모듈이름/별칭.변수/함수()/클래스

# 모듈 등록 2
# from 모듈이름 import 메소드1,[메소드2/함수/클래스...]
# from 모듈이름 import *
# 모듈이름 없이 변수, 함수, 클래스를 사용
# 변수 함수() 클래스

# dir함수 
# 네임스페이스에 등록되어 있는 모든 이름들을 리스트로 반환
dir()

# help함수
# 대화형 도움말 시스템 호출 또는 클래스나 메소드의 사용방법 반환
help(print)

# 네임스페이스
# 특정 객체를 이름에 따라 구분할 수  있는 범위

# 지역 네임스페이스
# 함수 또는 메